# 网站运行主体

这里是当前网站的运行根目录。

常用命令：

```powershell
npm start
npm run dev
npm run check
```

主要入口：

- `server.js`：Express 服务入口。
- `public/`：前台和后台页面。
- `data/archive.db`：SQLite 主数据库。
- `scripts/`：作品导入、审计、系列结构处理等脚本。

注意：上传素材已经移动到 `../制作网站素材图片/02-后台上传素材/`，`server.js` 已同步改为从该目录提供 `/uploads/...`。
